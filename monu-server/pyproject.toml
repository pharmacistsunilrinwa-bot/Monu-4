[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "monu-server"
version = "0.1.0"
description = "Modular Personal AI Operating Server"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["app"]
